# 如何启动 Chord 模拟器

## 🚀 快速启动

### 方式 1: 使用脚本（最简单）

```bash
cd ex03/task1
chmod +x run_gui.sh
./run_gui.sh
```

### 方式 2: 直接使用命令

```bash
cd ex03/task1
./gradlew build -x test -q && java -cp "build/classes/java/main:build/resources/main:$(find ~/.gradle/caches/modules-2/files-2.1 -name '*.jar' | tr '\n' ':')" ch.unibas.dmi.dbis.fds.p2p.ui.Main --bits=3 --dynamic=false
```

### 方式 3: 分步执行

```bash
cd ex03/task1

# 第一步：编译项目
./gradlew build -x test

# 第二步：运行应用程序
java -cp "build/classes/java/main:build/resources/main:$(find ~/.gradle/caches/modules-2/files-2.1 -name '*.jar' | tr '\n' ':')" ch.unibas.dmi.dbis.fds.p2p.ui.Main --bits=3 --dynamic=false
```

---

## 📝 参数说明

### --bits (标识符空间位数)

- `--bits=3` (默认) - 3 位标识符空间，节点 ID 范围 0-7
- `--bits=4` - 4 位标识符空间，节点 ID 范围 0-15
- `--bits=5` - 5 位标识符空间，节点 ID 范围 0-31

### --dynamic (动态稳定化)

- `--dynamic=false` (默认) - 静态模式，节点加入后不再变化
- `--dynamic=true` - 动态模式，使用周期性稳定化

---

## 🎯 常用命令

### 启动 3 位静态模式（推荐用于学习）
```bash
./run_gui.sh --bits=3 --dynamic=false
```

### 启动 4 位静态模式
```bash
./run_gui.sh --bits=4 --dynamic=false
```

### 启动 3 位动态模式
```bash
./run_gui.sh --bits=3 --dynamic=true
```

### 启动 5 位动态模式
```bash
./run_gui.sh --bits=5 --dynamic=true
```

---

## ⚠️ 常见问题

### Q1: 出现 "缺少 JavaFX 运行时组件" 错误

**原因**: JAR 文件不包含 JavaFX 模块信息

**解决方案**: 使用上面的命令方式 2 或 3，它们会正确加载 JavaFX 依赖

### Q2: 出现 "Module javafx.web not found" 错误

**原因**: JavaFX 模块路径配置不正确

**解决方案**: 使用脚本方式 1 或命令方式 2，它们会自动处理依赖

### Q3: 应用程序启动很慢

**原因**: 第一次启动需要编译项目和加载所有依赖

**解决方案**: 这是正常的，后续启动会更快。可以使用 `-q` 参数减少输出

### Q4: 没有看到 GUI 窗口

**原因**: 可能是因为没有图形界面环境或窗口在后台

**解决方案**: 
- 检查是否有 GUI 窗口打开（可能在其他桌面）
- 查看终端输出是否有错误信息
- 尝试使用 `--bits=3 --dynamic=false` 参数

---

## 🔍 验证启动成功

如果看到以下日志信息，说明启动成功：

```
Starting Chord simulator with config: ChordConfiguration{bits=3, dynamic=false}
```

---

## 📊 GUI 应用程序功能

启动后，你会看到一个 GUI 应用程序，可以：

1. **可视化 Chord 网络** - 看到节点在圆形上的分布
2. **添加节点** - 动态添加新节点到网络
3. **查看手指表** - 检查每个节点的手指表
4. **查看数据分配** - 看到数据如何分配到各个节点
5. **执行查询** - 测试数据查询功能

---

## 💡 建议

### 用于学习
```bash
./run_gui.sh --bits=3 --dynamic=false
```
- 3 位空间足够理解 Chord 协议
- 静态模式便于观察节点加入过程

### 用于演示
```bash
./run_gui.sh --bits=4 --dynamic=false
```
- 4 位空间提供更多节点
- 静态模式便于控制演示流程

### 用于测试稳定化
```bash
./run_gui.sh --bits=3 --dynamic=true
```
- 动态模式测试周期性稳定化
- 观察网络如何自我修复

---

## 🛠️ 故障排除

### 如果脚本不可执行

```bash
chmod +x run_gui.sh
```

### 如果需要清理缓存

```bash
./gradlew clean
./gradlew build -x test
```

### 如果需要完整重建

```bash
rm -rf build
./gradlew build -x test
```

---

## 📚 相关文档

- `README.md` - 项目说明
- `NODE_JOIN_DETAILED_WALKTHROUGH.md` - 节点加入详细推导
- `NODE_JOIN_VISUAL_GUIDE.md` - 节点加入可视化指南

---

## ✅ 总结

| 方式 | 命令 | 难度 | 推荐场景 |
|------|------|------|---------|
| 脚本 | `./run_gui.sh` | ⭐ 简单 | 日常使用 |
| 直接命令 | 方式 2 | ⭐⭐ 中等 | 自定义参数 |
| 分步执行 | 方式 3 | ⭐⭐⭐ 复杂 | 调试问题 |

**推荐**: 使用脚本方式 1，最简单方便！

